export type FriendItem = { id: number; name: string; message: string };
export type ChatItem = { id: number; room: string; message: string; time: string; unread: number };
export type OpenFeedItem = { id: number; title: string; likes: number; comments: number; summary: string };
